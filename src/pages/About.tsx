export function About() {
  return (
    <div>
      <h1>About</h1>
      <p>
        This page exists to test client-side routing. Load this URL
        directly (not by clicking the nav link) after deploying — if it
        returns a 404 instead of this page, the host needs an SPA rewrite
        rule (see <code>public/.htaccess</code> in this repo).
      </p>
    </div>
  )
}
